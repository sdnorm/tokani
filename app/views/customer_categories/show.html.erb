<%= content_for :title, "Customer Categories ##{@customer_category.display_value}" %>
<%= turbo_stream_from @customer_categories %>
<%= render partial: "shared/sidenav" %>
<div class="flex flex-1 flex-col md:pl-64">
  <main class="flex-1">
    <div class="py-6">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 md:px-8">
          <div class="px-4 sm:px-6 lg:px-8">
            <div class="sm:flex sm:items-center pb-6">
                <div class="sm:flex-auto"> 
                  <h1 class="text-xl font-semibold text-gray-900">
                    <%= link_to "Customer Categories", customer_categories_path, class: "text-tokanisecondary-500" %>
                      <span class="text-gray-400 font-light mx-2">\</span>
                      <%= @customer_category.display_value %>
                  </h1>
                </div>
                  <div class="mt-4 sm:mt-0 sm:ml-16 sm:flex-none">
                    <%= link_to t("edit"), edit_customer_category_path(@customer_category), class: "btn btn-tokani_purple" %>
                  </div>
                </div>
          

                <div class="p-8 bg-white rounded shadow">
                  <%= render partial: 'customer_category', locals: { customer_category: @customer_category} %>
                </div>
            </div>
          </div>
        </div>
  </main>
</div>



