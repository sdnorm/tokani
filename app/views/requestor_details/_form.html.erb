<%= form_with(model: requestor_detail) do |form| %>
  <%= render "shared/error_messages", resource: form.object %>

  <div>
    <%= form.button button_text(form.send(:submit_default_value)), class: "btn btn-primary" %>

    <% if form.object.new_record? %>
      <%= link_to t("cancel"), requestor_details_path, class: "btn btn-white" %>
    <% else %>
      <%= link_to t("cancel"), requestor_detail_path(@requestor_detail), class: "btn btn-white" %>
    <% end %>
  </div>
<% end %>
