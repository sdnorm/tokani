<%= content_for :title, "Bill Rates ##{@bill_rate.id}" %>
<%= turbo_stream_from @bill_rate %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Bill Rates", bill_rates_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      Bill Rate #<%= @bill_rate.id %>
    </h1>
    <%= link_to t("edit"), edit_bill_rate_path(@bill_rate), class: "btn btn-white" %>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render @bill_rate %>
  </div>
</div>
