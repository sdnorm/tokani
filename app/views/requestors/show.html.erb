
<%= turbo_stream_from @requestor %>
<%= render partial: "shared/sidenav" %>
<div class="flex flex-1 flex-col md:pl-64">
  <main class="flex-1">
    <div class="py-6">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 md:px-8">
          <div class="px-4 sm:px-6 lg:px-8">
            <div class="sm:flex sm:items-center pb-6">
                <div class="sm:flex-auto"> 
                  <h1 class="text-xl font-semibold text-gray-900">
                    <%= link_to "Requestors", requestors_path, class: "text-tokanisecondary-500" %>
                      <span class="text-gray-400 font-light mx-2">\</span>
                      <%= @requestor.last_name %>, <%= @requestor.first_name %>
                  </h1>
                </div>
                  <div class="mt-4 sm:mt-0 sm:ml-16 sm:flex-none">
                    <%= link_to t("edit"), edit_requestor_path(@requestor), class: "btn btn-tokani_purple" %>
                  </div>
                </div>
          

                <div class="p-8 bg-white rounded shadow">
                  <%= render partial: 'requestor', locals: { requestor: @requestor} %>
                </div>
            </div>
          </div>
        </div>
  </main>
</div>

