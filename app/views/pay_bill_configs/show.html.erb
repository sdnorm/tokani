<%= content_for :title, "Pay Bill Configs ##{@pay_bill_config.id}" %>
<%= turbo_stream_from @pay_bill_config %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Pay/Bill Configs", pay_bill_configs_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      <%= @pay_bill_config.name %>
    </h1>
    <%= link_to t("edit"), edit_pay_bill_config_path(@pay_bill_config), class: "btn btn-white" %>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render @pay_bill_config %>
  </div>
</div>
