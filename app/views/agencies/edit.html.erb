<%= content_for :title, t("shared.scaffold.edit.title", model: "Agency") %>
<%= content_for :title, "Agencies ##{@agency.id}" %>
<%= turbo_stream_from @agency %>
<div class="flex flex-1 flex-col">
  <main class="flex-1">
    <div class="py-6">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 md:px-6">
        <div class="px-4 sm:px-6 lg:px-8">
          <div class="sm:flex sm:items-center pb-6">
            <div class="sm:flex-auto"> 
              <h1 class="text-xl font-semibold text-gray-900">
                <%= link_to "Agencies", agencies_path, class: "text-tokanisecondary-500" %>
                <span class="text-gray-400 font-light mx-2">\</span>
                <%= @agency.name %>
              </h1>
            </div>
          </div>
          <div class="p-8 bg-white rounded shadow">
            <%= render "tokani_form", agency: @agency %>
          </div>
        </div>
      </div>
    </div>
  </main>
</div>

